resources:
  - ../../manager.yaml

apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
images:
- name: controller
  newName: "{{ .Values.image.operatorImage }}"
  newTag: "{{ .Values.image.operatorImagePullPolicy }}"

namespace: "{{ .Release.Namespace }}"

patchesJson6902:
- target:
    group: apps
    version: v1
    kind: Deployment
  patch: replace_values.yaml
